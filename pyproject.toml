[build-system]
requires = ["flit_core >=3.2,<4"]
build-backend = "flit_core.buildapi"

[project]
name = "Grapeseed County RP Docs"
authors = [{name = "Badnit", email = "operator@gcrp.city"}]
dynamic = ["version", "description"]
